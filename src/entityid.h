/** @file entityid.h
 *  @brief Shared entity id alias and invalid-id sentinel.
 */

#pragma once

/// @brief Integer identifier type used for all runtime entities.
typedef int entityid;
/// @brief Canonical invalid entity id sentinel.
#define ENTITYID_INVALID -1
