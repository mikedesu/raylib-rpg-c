/** @file tile_id.h
 *  @brief Alias for tile identifiers and invalid sentinel.
 */

#pragma once

/// @brief Integer identifier type for tiles in legacy/helper code.
typedef int tile_id;
/// @brief Invalid tile id sentinel.
#define TILE_ID_INVALID -1
